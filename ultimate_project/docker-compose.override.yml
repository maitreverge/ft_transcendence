services:
  nginx:
    build:
      args:
        env: dev
    volumes:
      - ./staticfiles:/usr/share/nginx/static   

  static_files:
    environment:
      - env=dev
    volumes:    
      - ./static_files:/app/

  tournament:
    environment:
      - env=dev
    volumes:
      - ./tournament:/app/

  match:
    environment:
      - env=dev
    volumes:
      - ./match:/app/
